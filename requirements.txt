# =============================================================================
# PROVENANCE URL CHECKER - REQUIREMENTS (v6.0)
# =============================================================================
#
# NEW in v6.0:
#   - PyPDF2 (PDF metadata extraction)
#   - GitHub API support (uses existing requests library)
#   - Dataset API handlers (CKAN, Socrata, ArcGIS - uses existing requests)
#   - Portal handlers (Census, EPA, NASA - uses existing beautifulsoup4)
#
# Installation:
#   pip install -r requirements.txt
#
# =============================================================================

# =============================================================================
# Core Dependencies
# =============================================================================

# HTTP requests library
requests>=2.28.0

# HTTP client library (used for retry logic and SSL warnings)
urllib3>=2.0.0

# YAML configuration parsing
PyYAML>=6.0

# Date parsing utilities
python-dateutil>=2.8.0

# =============================================================================
# HTML/Web Scraping Dependencies
# =============================================================================

# HTML parsing library
beautifulsoup4>=4.11.0

# Fast XML/HTML parser (used by BeautifulSoup)
lxml>=4.9.0

# =============================================================================
# NEW v6.0: PDF Processing Dependencies
# =============================================================================

# PDF metadata extraction (for method_pdf_metadata)
PyPDF2>=3.0.0

# =============================================================================
# Browser Automation (Selenium)
# =============================================================================

# Selenium WebDriver for browser automation
selenium>=4.8.0

# Automatic WebDriver management (downloads ChromeDriver automatically)
webdriver-manager>=4.0.0

# =============================================================================
# Development & Testing Dependencies
# =============================================================================

# Testing framework
pytest>=7.0.0

# Test coverage reporting
pytest-cov>=4.0.0

# Mock HTTP responses for testing
responses>=0.22.0

# HTTP mocking library
requests-mock>=1.10.0

# =============================================================================
# LLM Verification (Groq API)
# =============================================================================

# Groq API client for timestamp verification
groq>=0.4.0

# Environment variable management
python-dotenv>=1.0.0

# =============================================================================
# Groq Compound (Built-in Browser Automation)
# =============================================================================

# pandas for CSV processing
pandas>=2.0.0

# =============================================================================
# Optional Dependencies
# =============================================================================

# For colored console output (optional)
colorama>=0.4.6

# For progress bars (optional)
tqdm>=4.65.0

# =============================================================================
# v6.0 NOTES: Additional Configuration
# =============================================================================

# GITHUB_TOKEN (Optional - for Git repository analysis method)
# - Not a library, but an environment variable
# - Add to .env file: GITHUB_TOKEN=your_github_personal_access_token
# - Without token: Git method will work but with rate limits (60 requests/hour)
# - With token: 5000 requests/hour
# - Get token from: https://github.com/settings/tokens

# DATASET API METHODS (No additional libraries needed)
# - CKAN API: Uses requests library (already installed)
# - Socrata API: Uses requests library (already installed)
# - ArcGIS API: Uses requests library (already installed)

# PORTAL HANDLERS (No additional libraries needed)
# - Census, Data.gov, EPA, NASA handlers use beautifulsoup4 (already installed)
